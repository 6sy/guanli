<template>
  <div class='views'
       :style='{background:background}'
       v-show='isShow'>
    <div class='ring'
         :style='{width:radius,height:radius,lineHeight:radius,color:color}'>
      {{text}}
      <span></span>
    </div>
    <div class='test'></div>
  </div>
</template>
<script>
export default {
  props: {
    // 背景颜色
    background: {
      type: String,
      default: 'rgba(0, 0, 0,0.3)'
    },
    radius: {
      String,
      default: '150px'
    },
    text: {
      type: String,
      default: 'loading'
    },
    color: {
      type: String,
      default: '#fff000'
    },
    borderColor: {
      type: String,
      default: '#fff000'
    },
    isShow: {
      type: Boolean,
      default: false
    }
  },
  mounted () {
  }
}
</script>
<style scoped>
.views {
  width: 100%;
  height: 100vh;
}
.ring {
  position: absolute;
  top: 50%;
  left: 50%;
  letter-spacing: 4px;
  transform: translate(-50%, -50%);
  width: 150px;
  height: 150px;
  background: transparent;
  border: 3px solid #3c3c3c;
  border-radius: 50%;
  text-align: center;
  font-family: sans-serif;
  line-height: 150px;
  font-size: 20px;
  color: #fff000;
  text-transform: uppercase;
  text-shadow: 0 0 10px #fff000;
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);

  /* animation: animateCircle 2s linear infinite; */
}
.ring:before {
  content: "";
  position: absolute;
  top: -3px;
  left: -3px;
  width: 150px;
  height: 150px;
  border: 3px solid transparent;
  /* 旋转的颜色 */
  border-top: 3px solid #fff000;
  border-right: 3px solid #fff000;
  border-radius: 50%;
  animation: animateCircle 2s linear infinite;
}
/* .ring span {
  display: block;
  position: absolute;
  top: calc(50%);
  left: 50%;
  width: 50%;
  height: 4px;
  background: transparent;
  transform-origin: left;
  animation: animate 2s linear infinite;
}
.ring span::before {
  content: "";
  position: absolute;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: #fff000;
  top: -6px;
  right: -8px;
  box-shadow: 0 0 20px #fff000;
} */
@keyframes animate {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(360deg);
  }
}
@keyframes animateCircle {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(360deg);
  }
}
</style>
